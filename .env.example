# Copy to .env and fill in real values. Never commit .env.
# For `node index.js --dry-run` only GROQ_API_KEY is required.

GROQ_API_KEY=
EMAIL_USER=
EMAIL_PASS=
EMAIL_TO=

# Optional: override default from agent.config.json
# MAX_OUTREACH_EMAILS=10
